<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.renren.app.dao.ChMatchDao">
    <!--<select id="selectMacthForPage" parameterType="java.util.Map" resultType="java.util.Map">
        select
          m.id as id,
          m.title ,
          m.content ,
          m.img_url as imgUrl,
          m.views
        from ch_match as m
        left join ch_category as c on c.id=m.category_id
    </select>-->

</mapper>
